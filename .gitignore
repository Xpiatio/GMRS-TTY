# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/

# Virtual environment
.venv/
venv/
env/

# Local per-user state (copy config.example.json to config.json and edit)
config.json
contacts.json

# Voice models (download separately — see README)
Voices/

# Bundled ML models (Stage 6 — air-gapped install)
Models/

# Build artifacts (.deb staging tree, vendored wheels, output packages)
build/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Claude Code local settings
.claude/settings.local.json
